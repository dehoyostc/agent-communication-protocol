# AF-011-orphaned-divergence-refs: Orphaned divergence references must warn, not crash

Tests a divergence referencing a missing/expired object. The engine should emit a lifecycle warning and continue.
